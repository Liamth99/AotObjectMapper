@page "/IgnoreMembers"

<PageTitle>Ignore Members</PageTitle>

<p>Specific members can be ignored for a mapping. The example below skips Id and MiddleName.</p>

<Markdown>
```csharp
[GenerateMapper]
[Map&lt;User, UserDto&gt;(ignoredMembers: [nameof(User.Id), nameof(User.MiddleName)])]
public partial class UserMapper;
```
</Markdown>